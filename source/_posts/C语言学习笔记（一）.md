---
title: C语言学习笔记（一）
toc: true
indent: true
top: false
comments: true
archive: true
cover: false
mathjax: false
pin: false
top_meta: false
bottom_meta: false
sidebar:
  - toc
tags:
  - C语言
  - 《C和指针》        
categories: 编程语言
keywords:
  - C语言
  - 编译流程
  - 程序内存布局
  - 内存
updated: ''
date: 2023-11-07 17:33:24
img: /medias/featureimages/0.webp
summary: C 语言编译流程与程序内存布局
---
# 编程语言
## C语言学习笔记
### C语言学习笔记（一）
#### 1.编译流程
**①引言**
>**概述**：编译就是将**源代码**如`.c`文件转换为计算机能够识别并执行的**机器指令**，最终生成`.exe`和`.out`等**可执行文件**
{%list%}
完整的汇编过程通常可分为预处理、编译、汇编和链接四个阶段，这里以Ubuntu 22.04环境下的GCC工具链为例
{%endlist%}
{%right%}
如果不需要关注中间过程，可以直接使用gcc hello.c -o hello完成对以下脚本的整个编译流程，并生成可执行文件hello
{%endright%}
>编译完成后，直接在当前文件夹下运行`./hello`即可运行该可执行文件
{%warning%}
在执行上述操作之前，需要确保系统安装了GCC及相关编译工具，在Ubuntu 22.04可以安装build-essential软件包
{%endwarning%}
```c
#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}
```
**②预处理**
>**概述**：预处理阶段对应的`GCC`命令为`gcc -E hello.c -o hello.i`，主要处理源文件中以`#`开头的**预处理指令**并生成预处理后的`.i`文件
{%list%}
预处理的核心工作为宏定义与展开、文件包含、条件编译和注释移除等，分别对应#define、#include、#if和#ifdef等指令
{%endlist%}
{%right%}
预处理阶段并不会进行完整的语法检查和类型检查，本质上是进行一些文本替换和清理工作
{%endright%}
{%warning%}
打开hello.i可以看到该文件被拓展到几百行，新增的代码主要是预处理器将stdio.h及其依赖无脑粘贴到对应位置
{%endwarning%}
```c
// 预处理前
#define PI 3.14159
#define MAX_BUFFER 1024

double area = PI * 2 * 2;
int buf[MAX_BUFFER];

// 预处理后
double area = 3.14159 * 2 * 2;
int buf[1024];
```
**③编译**
>**概述**：编译阶段对应的`GCC`命令为`gcc -S hello.i -o hello.s`，主要是将**C语言代码**转化为对应架构的**汇编代码**，并进行**一定的优化**
{%list%}
编译阶段的核心工作包括词法分析、语法分析、语义分析、中间代码生成与代码优化，最终生成对应目标架构的汇编代码
{%endlist%}
>比如说**非法字符**、**缺少分号**、**类型不匹配**、**未声明变量**和**未使用变量**等错误都是出现在编译阶段
{%right%}
编译器通常会对代码进行常量折叠、死代码消除、函数内联等优化，可以通过-O0、-O1、-O2、-O3等选项控制优化等级
{%endright%}
>`-O0`：关闭大多数性能优化，仅完成**代码生成所必需的转换**，尽量保持源码结构**便于调试**

>`-O1`：启用**较基础的优化**，如**常量传播**、**死代码删除**、**简单表达式化简**和**部分寄存器分配优化**，在较低开销下提升性能

>`-O2`：在`-O1`基础上启用更多全局和循环优化，如**公共子表达式消除**、更多**代码移动**、**分支优化**和更积极的**数据流分析**

>`-O3`：在`-O2`基础上进一步采用更激进的性能优化，如更积极的**函数内联**、**循环展开**、**循环向量化**和自动`SIMD`优化
{%warning%}
若编译器判断某个变量在代码执行阶段不会被修改，则会将其存入寄存器中提升访问速度，但不一定是合理的
{%endwarning%}
{%wrong%}
编译器只会考虑软件逻辑，所以当一个变量会被硬件、中断或者其他线程修改时进行上述优化会出现逻辑错误
{%endwrong%}
```c
/* 常量折叠：将能提前计算好的值计算好 */
// 优化前
int seconds_per_day = 60 * 60 * 24;
int a = 10;
int b = a + 5;
// 优化后
int seconds_per_day = 86400; // 直接算出结果
int b = 15;                  // 变量 a 被传播并替换
```
```c
/* 函数内联：因为函数调用是有开销的，所以对于短小且频繁调用的函数，编译器会直接展开 */
// 优化前
inline int square(int x) { return x * x; }
int main() {
    int res = square(5);
}
// 优化后
int main() {
    int res = 25; // 先内联变成了 5 * 5，再被常量折叠为 25
}
```
```c
/* 寄存器优化：将不会被代码改变的变量存入寄存器中 */
// 优化前
uint32_t *status_reg = (uint32_t *)0x40021000;
while (*status_reg == 0) {
    // 死循环，等待硬件将寄存器的值置为 1
}
//优化后
uint32_t temp = *status_reg; // 只去内存读一次，随后把值存到CPU内部寄存器中
while (temp) {
    // 无限死循环，再也不会去读取 0x40021000 这个内存地址了！
}
```
**④汇编**
>**概述**：汇编阶段对应的的`GCC`命令为`gcc -c hello.c -o hello.o`，主要是将**汇编代码**转化为**机器语言**，并生成目标文件`.o`
{%list%}
汇编器生成的目标文件在Linux中通常采用ELF格式，其中包含.text、.data、.rodata和.bss等不同的节
{%endlist%}
{%right%}
可以使用objdump -d hello.o对目标文件中的机器指令进行反汇编，结果通常依次显示指令地址、机器码和对应的汇编指令
{%endright%}
{%warning%}
目标文件中的指令地址通常为相对地址，而暂时无法确定的地址会保留占位值并记录重定位信息，等待链接阶段统一解析和修正
{%endwarning%}
{%wrong%}
每个.c文件通常是独立编译的，汇编阶段只能确定当前文件内部函数和变量的位置，外部符号仍需在链接阶段解析
{%endwrong%}
```nasm
hello.o:     file format elf64-x86-64


Disassembly of section .text:

0000000000000000 <main>:
   0:	f3 0f 1e fa          	endbr64 
   4:	55                   	push   %rbp
   5:	48 89 e5             	mov    %rsp,%rbp
   8:	48 8d 05 00 00 00 00 	lea    0x0(%rip),%rax        # f <main+0xf>
   f:	48 89 c7             	mov    %rax,%rdi
  12:	e8 00 00 00 00       	call   17 <main+0x17>
  17:	b8 00 00 00 00       	mov    $0x0,%eax
  1c:	5d                   	pop    %rbp
  1d:	c3                   	ret    
```
**⑤链接**
>**概述**：链接阶段对应的`GCC`命令为`gcc hello.o -o hello`，主要是将**目标文件**和所需的**库文件**组合拼装为一个完整的**可执行文件**
{%list%}
目标文件中包含符号表，记录当前文件定义或引用的函数、全局变量等符号，以及这些符号的类型、位置和链接属性
{%endlist%}
>对于需要**跨文件链接**的符号，可以分为**强符号**和**弱符号**，用于描述符号的**链接可见性**，以及出现**同名符号**时的解析规则

>**普通函数**和**全局变量**通常产生**强符号**，**弱符号**通常需要借助`GCC`的`__attribute__((weak))`等编译器扩展**显式声明**
{%right%}
链接阶段的核心工作主要包括符号解析和重定位，前者负责找到每个外部符号对应的定义，后者负责确定代码和数据的地址布局
{%endright%}
>**符号解析**：将各个`.o`文件中对**函数和全局变量**的引用，与**其他目标文件或库**中对应的符号定义建立关联

>**重定位**：将多个`.o`文件中**相同的段**合并随后为所有的符号分配**绝对地址**，最后根据重定位信息**填补占位符**
{%warning%}
链接器通常不允许出现多个同名的强符号定义，如果一个强符号与多个弱符号同名，则会优先选择强符号
{%endwarning%}
{%wrong%}
如果存在多个同名的弱符号定义，链接器通常会选择其中一个，但具体结果可能受链接顺序和工具链影响，容易导致隐蔽bug
{%endwrong%}
```c
/* ------ main.c ------ */
int x = 10; // 强符号：初始化的全局变量
void foo();

int main() {
    foo();
    return 0;
}

/* ------ utils.c ------ */
int x;      // 弱符号：未初始化的全局变量（名字和 main.c 里的 x 冲突）
            // 链接器会默默将此处的 x 映射到 main.c 里的 x 的地址！

void foo() { // 强符号
    x = 20;  // 表面上改的是 utils.c 的 x，实际把 main.c 的 x 给改了！
}
```
**⑥程序内存布局**
>**概述**：一个典型的**用户态进程内存布局**如下图所示，主要由`.text`、`.rodata`、`.data`、`.bss`、`heap`和`stack`组成
{%list%}
程序运行后，不同类型的代码和数据通常会被组织到不同的内存区域，不同区域具有不同的用途、访问权限、生命周期和管理方式
{%endlist%}
{%right%}
这种分区方式便于操作系统对代码和数据进行独立的加载、保护、分配与回收，提高内存管理效率并增强程序运行时的安全性
{%endright%}
{%warning%}
该布局只是现代操作系统中常见的典型模型，实际地址分布会受到操作系统、处理器架构、编译器、链接方式和ASLR等因素影响
{%endwarning%}
```text
High Address  ┌──────────────────────────────────────┐
              │                                      │
              │                                      │   Stack
              │                Stack                 │   作用：保存函数调用过程中的运行时数据
              │                                      │   典型对象：局部变量、返回地址、栈帧
              │                                      │   特点：可读、可写、通常不可执行；通常向下增长
              ├──────────────────────────────────────┤
              │                                      │
              │                                      │   mmap / Shared Libraries
              │       mmap / Shared Libraries        │   作用：建立文件、匿名内存和动态库映射
              │                                      │   典型对象：动态库、文件映射、匿名映射
              │                                      │   特点：权限可配置，取决于具体映射属性
              ├──────────────────────────────────────┤
              │                                      │
              │                                      │   Heap
              │                 Heap                 │   作用：提供运行时动态内存分配
              │                                      │   典型对象：malloc/calloc/realloc 分配的对象
              │                                      │   特点：可读、可写、通常不可执行；通常向上增长
              ├──────────────────────────────────────┤
              │                                      │
              │                                      │   .bss
              │                  .bss                │   作用：保存零初始化的静态存储期数据
              │                                      │   典型对象：未初始化或零初始化的全局/static 变量
              │                                      │   特点：可读、可写、通常不可执行；加载时清零
              ├──────────────────────────────────────┤
              │                                      │
              │                                      │   .data
              │                 .data                │   作用：保存已初始化的可写静态数据
              │                                      │   典型对象：已初始化的全局变量、static 变量
              │                                      │   特点：可读、可写、通常不可执行；初值存于程序镜像
              ├──────────────────────────────────────┤
              │                                      │
              │                                      │   .rodata
              │                .rodata               │   作用：保存程序中的只读数据
              │                                      │   典型对象：字符串字面量、只读常量数据
              │                                      │   特点：可读、不可写、通常不可执行
              ├──────────────────────────────────────┤
              │                                      │
              │                                      │   .text
              │                 .text                │   作用：保存程序编译后的机器指令
              │                                      │   典型对象：函数代码、可执行指令
Low Address   │                                      │   特点：可读、可执行、不可写
              └──────────────────────────────────────┘
```

